clear 

ssc install mdesc // Command to look for missing values in each variable of the dataset

ssc install fre // Useful command that sometimes can be used instead of tabulate 

net install http://www.stata.com/users/kcrow/tab2xl, replace

ssc install groupfunction // Command created by Paul Corral from World Bank (and others) that's similar to the collapse comand. It is useful to replicate the code from the report "Small Area Estimations for Poverty Mapping" 